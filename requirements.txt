# 사우디 크롤러 의존성 — 버전 고정
# Python 3.12 기준. Actions 러너와 로컬이 동일 환경이 되도록 유지한다.
# 최종 점검: 2026-04-10

# ─── HTTP / 크롤링 ────────────────────────────────────
httpx[http2]>=0.27.2
beautifulsoup4>=4.12.3
lxml>=5.3.0
trafilatura==2.0.0          # Triple Fallback 3차: 본문 자동 추출 (아랍어 지원)

# 헤비 크롤링이 필요한 경우에만 (Render 등 분리 배포 환경)
# playwright==1.45.0
# playwright-stealth==1.0.6

# ─── 재시도·레이트 리밋 ───────────────────────────────
tenacity>=9.0.0

# ─── INN 정규화 (약품명 WHO 표준명 매칭) ─────────────
preon==0.1.2                        # 3단계 매칭: exact → partial → fuzzy
jellyfish>=1.2.1                    # preon 의존: 문자열 유사도

# ─── 데이터 처리 ──────────────────────────────────────
pandas>=2.2.3
# polars==1.12.0      # 선택, 고성능 필요 시

# ─── 금융 시세 (대시보드 환율) ─────────────────────────
# SAR/KRW 는 티커 없음 → USDKRW + USDSAR 경유 계산
yfinance>=0.2.40

# ─── DB ────────────────────────────────────────────────
supabase>=2.9.0
postgrest>=0.17.2

# ─── LLM (AI 판단 레이어) ─────────────────────────────
# 프로젝트 헌법: Haiku / mini 만 허용
anthropic>=0.39.0

# ─── 웹 서버 (대시보드) ────────────────────────────────
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9   # Form/File (POST /api/p2/price-analyze 등)
python-docx>=1.1.0

# ─── 설정 / 로깅 ──────────────────────────────────────
pyyaml>=6.0.2
python-dotenv>=1.0.1
loguru>=0.7.2

# ─── CAPTCHA (사전 허가된 소스에만) ───────────────────
# 2captcha-python==1.5.1   # 필요 시 주석 해제

# ─── PDF 처리 (MOH Newsroom 등 문서 소스) ─────────────
# pdfplumber==0.11.4       # 필요 시 주석 해제

# ─── 테스트 ──────────────────────────────────────────
pytest>=8.3.3
pytest-httpx>=0.32.0
